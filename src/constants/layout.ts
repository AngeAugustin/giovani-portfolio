/** Conteneur horizontal partagé (header, pages, footer). */
export const containerClass = 'mx-auto w-full max-w-6xl px-4 sm:px-6 lg:px-8'

/** Conteneur large pour heroes / grilles portfolio. */
export const containerWideClass = 'mx-auto w-full max-w-7xl px-4 sm:px-6 lg:px-8'
